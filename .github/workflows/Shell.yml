name: shell command
on : push

jobs:
  run-shell-command:
   runs-on: ubuntu-latest
   steps :
   - name : String
     run : echo "Hello World"
   -  name : Multiline
      run : |
       node -v
       npm -v 
  
